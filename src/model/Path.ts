import path from 'path'
import { DirPath } from '../../config.js'
export default {
    image: path.join(DirPath, 'public', 'image', 'image'),
    imageText: path.join(DirPath, 'public', 'image', 'image.text'),
}